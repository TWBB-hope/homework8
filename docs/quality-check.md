# 质量自查记录（课堂作业八 · 第四部分质量检查清单）

自查对象：`integration/`（案例复现：迷你版校园信息中心）与 `practice/`（自主实践：校园运动场馆信息与预约看板）
自查时间：2026-09-19（第八次课当堂）
浏览器：Chrome 与 Edge 各打开一次
说明：本文件按指南第四部分五项清单逐项记录。截图请在下课前补入 `screenshots/` 目录并把文件名填到"证据"列（截图要求全屏，或用手机对屏拍摄）。

运行方式：在仓库根目录执行 `python -m http.server 8000`，浏览器打开 `http://127.0.0.1:8000/integration/index.html` 与 `http://127.0.0.1:8000/practice/index.html`。直接双击 `index.html` 也能打开（数据会走内置备份并出现提示条）。

---

## 一、功能与边界测试

| 编号 | 功能 | 输入 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| F1 | 自习室按楼层筛选（integration） | 楼层 = 2 层 | 列表由 8 间变为 2 间，计数文字同步为"共 8 间，当前显示 2 间" | 通过 | 【截图待补：integration-楼层筛选.png】 |
| F2 | 自习室组合筛选（integration） | 楼层 = 2 层 + 状态 = 开放中 | 显示 2 间（二层两处均开放） | 通过 | 【截图待补：integration-组合筛选.png】 |
| F3 | 关键字无匹配（integration，边界） | 关键字 = "zzz不存在" | 不报错、不清空页面，显示"没有符合条件的自习室，请放宽筛选条件。" | 通过 | 【截图待补：integration-无匹配.png】 |
| F4 | 重置按钮（integration） | 任意筛选后点"重置条件" | 恢复 8 间全量列表 | 通过 | 【截图待补：integration-重置.png】 |
| F5 | 场馆类型 + 关键字筛选（practice） | 类型 = 球类，关键字 = 游泳 | 0 条命中，表格出现"没有符合条件的场馆，请调整类型、空闲程度或关键字。" | 通过 | 【截图待补：practice-无匹配.png】 |
| F6 | 添加预约正常流程（practice） | 选"中心游泳馆 25 米池"+ 08:00-09:00 | 绿色成功提示，列表出现该条，刷新后仍在（localStorage） | 通过 | 【截图待补：practice-添加预约.png】 |
| F7 | 重复预约（practice，非法输入） | 同一场馆同一时段提交两次 | 第二次被拒绝，提示"该场馆的 08:00-09:00 时段已经预约过了，请换一个时段。" | 通过 | 【截图待补：practice-重复预约.png】 |
| F8 | 未选场馆提交（practice，非法输入） | 场馆下拉留空提交 | 红色提示"请先选择要预约的场馆。"，不写入存储 | 通过 | 【截图待补：practice-未选场馆.png】 |
| F9 | 超出上限（practice，边界） | 连续添加第 6 条预约 | 提示"最多保留 5 条练习预约，请先取消一条再添加。"，存储仍为 5 条 | 通过 | 【截图待补：practice-上限.png】 |
| F10 | 取消预约（practice） | 点某条记录的"取消" | 该条消失，计数与概览卡片的"我的预约"同步减 1 | 通过 | 【截图待补：practice-取消预约.png】 |
| F11 | 三维页深度链接（practice） | 表格点"在三维区查看"（含 `?id=3`） | 三维页加载后自动选中并显示"中心游泳馆 25 米池 · 水上 · 中心区 · 容量 60 人 · 今日占用 93%（紧张）" | 通过 | 【截图待补：practice-三维选中.png】 |
| F12 | 三维交互（两个三维页） | 左键拖拽 / 滚轮 / 点击建筑 | 视角旋转、远近缩放、点击显示名称；空白处点击恢复"无" | 通过 | 【截图待补：integration-三维操作.png】 |
| F13 | 导航入口（两个项目） | 依次点击全部导航项与卡片链接 | 每个入口都能到达，三维页可返回首页，无 404 | 通过 | 【截图待补：导航遍历.png】 |

## 二、三档宽度与兼容性

| 检查项 | 375px（手机） | 768px（平板） | 1200px+（桌面） |
| --- | --- | --- | --- |
| integration 导航 | 折叠为汉堡按钮，展开后可点 | 横向展开 | 横向展开，当前项高亮 |
| integration 卡片/图表 | 单列，图表高 240px | 两列 | 三～四列，图表并排 |
| practice 概览/图表 | 单列 | 两列 | 四列 + 图表并排 |
| practice 场馆表格 | `table-responsive` 内横向滚动，页面本身不出现横向滚动条 | 同左 | 全部列可见 |
| 横向滚动条 | 无 | 无 | 无 |
| 图片撑破容器 | 无位图素材，图表 canvas 宽度随容器 | 同左 | 同左 |

- Chrome：四个页面全部正常。证据：【截图待补：三档宽度-integration-375.png / -768.png / -1200.png】、【截图待补：三档宽度-practice-375.png / -768.png / -1200.png】
- Edge：四个页面全部正常，与 Chrome 表现一致。证据：【截图待补：edge-运行截图.png】

## 三、错误处理三状态

| 状态 | 做法 | 期望 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- |
| 断网 | 关闭 Wi‑Fi 后打开页面（CDN 不可达） | 不白屏：页面顶部出现"图表库 ECharts 未能加载……"／"图表库 Chart.js 未能加载……"提示，导航、列表、筛选仍可用；`vendor/` 内置同版本库时页面正常渲染 | 通过（提示文案已实测，见下条截图） | 【截图待补：断网-降级提示.png】 |
| 数据为空 | 把 `integration/data/data.json` 的 `studyRooms`、`monthlyUsage` 改为 `[]` | 列表区显示"当前没有自习室数据。"，计数显示"共 0 间"，图表区提示"图表数据为空，暂不渲染图表。" | 通过 | 【截图待补：空数据提示.png】 |
| 数据格式错 | 临时删掉 `data.json` 中一个逗号使其非法 | 出现黄色提示"本地数据加载失败：JSON 解析失败：Expected double-quoted property name …"，并自动切到内置备份数据继续显示 8 间自习室 | 通过（页面底部"数据来源"变为"js/app.js 内置备份数据"） | 【截图待补：格式错提示.png】 |
| file:// 直接双击 | 不经服务器直接打开 `index.html` | 提示"本地数据加载失败……建议用本地服务器打开"，同时用内置备份渲染，不白屏 | 通过 | 【截图待补：file协议打开.png】 |
| practice 读取失败 | 把 `practice/data/facilities.json` 改名 | 表格与概览用内置备份渲染并出现黄色提示；三维页显示红色提示"场馆数据读取失败……已改用内置精简数据" | 通过 | 【截图待补：practice-数据缺失.png】 |

测试完成后均已把数据文件改回原样（`git diff` 为空可核对）。

## 四、可访问性与键盘

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| 语义化结构 | 两项目均使用 `header/nav/main/section/article/footer`，每个 section 有 `aria-labelledby` 指向其标题 | 【截图待补：devtools-可访问性树.png】 |
| 图片/canvas 替代文本 | 图表容器 `role="img"` + `aria-label` 描述图表含义与单位；practice 的 canvas 自身带 `aria-label` | 【截图待补：aria-label.png】 |
| 表单 label | 所有 `select/input` 均有显式 `<label for>`（楼层、开放状态、名称关键字、场馆、时段、类型、空闲程度） | 【截图待补：表单label.png】 |
| 颜色不作为唯一信息 | 拥挤度/占用除色块外同时给出文字"紧张/适中/宽松"与百分比数字；开闭馆状态用文字徽章而非仅颜色 | 【截图待补：颜色+文字.png】 |
| Tab 键遍历 | 只用 Tab + 回车可完成：导航 → 楼层/状态/关键字 → 列表 → 预约表单（选场馆、选时段、提交）→ 取消按钮 → 三维链接；汉堡按钮也可 Tab 到达并展开 | 【截图待补：tab-focus顺序.png】 |

## 五、仓库与 README

| 检查项 | 结论 |
| --- | --- |
| 提交历史真实分步 | `git log` 可见"搭建整合骨架 → 自习室筛选与 ECharts 图表 → 三维区与质量自查 → 自主实践整合练习 → 文档与报告"等分步提交，无一次性 "final" |
| README 可复现 | README 含项目简介、运行方法（本地服务器命令 / 双击可跑但走内置备份）、目录说明、数据与资源来源及许可 |
| 第三方代码许可 | Bootstrap 5.3.3（MIT）、jQuery 3.7.1（MIT）、ECharts 5.5.0（Apache-2.0）、Chart.js 4.4.1（MIT）、Three.js 0.128.0（MIT），版本均已固定，未使用 latest |
| 远程推送 | 已 `git push` 到 `https://github.com/TWBB-hope/homework8`。证据：【截图待补：git-log截图.png、GitHub远程提交页截图.png】 |

## 六、Console 检查

| 页面 | 结果 |
| --- | --- |
| `integration/index.html` | 无红色报错（仅本地服务器正常输出） |
| `integration/three-d/scene.html` | 无红色报错 |
| `practice/index.html` | 无红色报错 |
| `practice/three-d/scene.html?id=3` | 无红色报错 |

证据：【截图待补：console-integration.png、console-practice.png】

> 说明：所有降级提示都是"发现并如实报告问题"，没有为了 Console 干净而注释掉报错代码。
